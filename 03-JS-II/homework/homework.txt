1. En un archivo de texto separado que debes crear, escribe explicaciones de los siguientes conceptos como si se lo estuvieras explicando a un niño de 12 años. Hacer esto te ayudará a descubrir rápidamente cualquier agujero en tu comprensión.

* `for`: es un bucle que se ejecuta de manera constante, pero para eso toca dar una variable, una orden y una orden para
         que se detenga.

         ej: for(var i = 1; n <= n; i++ )



* `&&`, `||`, `!` : son operadores logicos y  se usan para concatenar varias expresiones


                   && Operador "y" (AND) 

                   || Operador "o" (OR)

                   ! Operador  "no" (NOT)

                    